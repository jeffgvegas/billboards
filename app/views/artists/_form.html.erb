<%= form_for(@artist) do |f| %>
  <%= f.select(:name, options_from_collection_for_select(Artist.all, "id", "name", @artist)) %>
  <%= f.select(:songs, options_from_collection_for_select(Song.all, "id", "title", @song)) %>
  <%= f.submit("save") %>
<% end %>
